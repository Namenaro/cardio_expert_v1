# Версия v1
## Сделано:
1) База данных структуры форм  
2) Обертка к ней 
3) Редактирование базы данных форм в приложении доктора DA3 
4) Приложение ручной разметки датасета экземпляров формы доктором  *[PARTIALLY DONE, Владислав, в др. репозитории]*
5) Библиотека пазлов  *[пока минимальный набор]*
6) Ее парсер в базу
7) Интеграция LUDB 
  
## Далее:
мелочи: 
   1.CORE: добавить в таблицу forms поля dataset_name_pos, dataset_name_neg; добавить в DA3 выбор доктором датасета для формы;
   2. DA3:  Изменить отрисовщик: +draw_signal, + draw_line, +draw_placeholder
1) CORE:  Обертка над датасетом экземпляров форм
2) CORE:  Пакет eval_exemplar:
      1) абстрактный класс Evaluator
      2) простейший наследник без обучения: непараметрическая гипотеза о принадлежности к распределению, min суммы p_values по параметрам формы; 
5) DA3:  Пакет **visualisation** : виджеты отладочной отрисовки рез-тов шага, трека, пазла, экземпляра, пула экземпляром 
6) CORE:  Пакет **run**: track_run, step_run ... -> запуск формы на сигнале c получением ExemplarsPool
7) DA3:  интеграция визуализации run в DA3 на все виджеты *[контроллер передает виджету объект с мин. интефейсом get_next()... тот его дергает и рисует вместе с листалкой], под каждый виджет свой объект*
8) CORE: Пакет **metrics**: ошибка расстановки точек; ошибка eval как классификатора;  Интеграция тестилки в DA3 
9)  CORE: Компилятор формы; Его интеграция в DA3
10) CORE:  Пакет **orcestrator** Орекастратор форм без обучения -> красивый репорт с полной разметкой в html/pdf 
11) Приложение программиста библиотеки пазлов, ADMIN_APP 


# Версия v2 
   1) Обучение:
      1) обучаемый eval
          1) на входе позитивные и негативные примеры устновки формы с сопоставленные параметры, а выход eval расцениваем как бинарный классификатор
          2) на входе реальная правильные и фактические постановки точек (только на positive датасете), рассм. задачу регрессии коррдинат точек
      3) обучаемый оркестратор -> диагнозы   (в которые можно и деревья, и графические модели,  нейросети...)
          1) обучаемая дискриминация между двумя формами, устновившимися с положительной оценкой на одном и том же фрагменте сигнала
  2) Конструктивный оркестратор c настоящим врачом ( if|else by doctor)
  3) интеграция ptb-xl и тест на его диагнозах
  4) полуавтоматическое ускоренное увеличение датасетов форм за счет частично-рабочих установщиков этих форм
  5) автоматическое структурное улучшение формы: пакет **relaxe_form**. Можно генетические алгтримы сюда или что-то "по сетке".
      1) авто упрощение избыточности формы (удалить лишние треки, пазлы и т.п., которые не улучшают качество).
      2) fine-tuning аргументов конструкторов в пазлах
    
  # Версия v3
  Клиент-серверное приложение для пользователя, загружающего ЭКГ и получающего красивый репорт с диагностикой и полным графическим и текстовым объяснением


  

    
   
